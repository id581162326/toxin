{% from 'assets/icons/index.njk' import check_mark %}

{% macro checkbox(name, appearance='default', label, description, checked, disabled) %}
  {% set checkboxLabel = 'checkbox__label' | uniqid %}
  {% set checkboxDesc = 'checkbox__description' | uniqid %}

  <label class="checkbox checkbox_appearance_{{ appearance }} {% if description %}checkbox_with_description{% endif %}">
    <input
      class="checkbox__input js-checkbox__input"
      name="{{ name }}"
      type="checkbox"
      aria-labelledby="{{ checkboxLabel }}"
      {% if description %}aria-describedby="{{ checkboxDesc }}" {% endif %}
      {% if checked %}checked{% endif %}
      {% if disabled %}disabled{% endif %}
    >
    <span class="checkbox__mark">
      {% if appearance==='default' %}
      {{ check_mark(class='checkbox__check-icon', width=10, height=8, gradient=true) }}
      {% else %}
      <span class="checkbox__inner-circle"></span>
      {% endif %}
    </span>
    <span class="checkbox__label" id="{{ checkboxLabel }}">{{ label }}</span>
    {% if description %}<span class="checkbox__description" id="{{ checkboxDesc }}">{{ description }}</span>{% endif %}
  </label>
{% endmacro %}
