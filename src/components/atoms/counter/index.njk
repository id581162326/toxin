{% macro counter(props) %}
  {% set counter_label_id = 'counter__input' | uniqid  %}

  {% set label = props.label %}
  {% set name = props.name %}
  {% set value = props.value %}
  {% set min = props.min %}
  {% set max = props.max %}

  <div class="counter js-counter">
    <label class="counter__label" for="{{ counter_label_id }}">{{ label }}</label>
    <button class="counter__control-btn js-counter__control-btn_is_decrement" type="button" tabindex="-1" aria-hidden="true">-</button>
    <input
      class="counter__input js-counter__input"
      value="{{ value }}"
      type="number"
      name="{{ name }}"
      min="{{ min }}"
      {% if max %}max="{{ max }}"{% endif %}
      id="{{ counter_label_id }}"
    >
    <button class="counter__control-btn js-counter__control-btn_is_increment" type="button" tabindex="-1" aria-hidden="true">+</button>
  </div>
{% endmacro %}